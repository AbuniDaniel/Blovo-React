import { Header } from '../../fragments/header';
import { LoginForm } from '../../loginForm/loginForm';

export const Login = () => {
  return (
    <div>
      <Header />
      <LoginForm />
    </div>
  );
}
