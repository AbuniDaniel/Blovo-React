import { Header } from '../../fragments/header';
import { Profile } from '../../profile/profile';

export const ProfileMain = () => {
  return (
    <div>
      <Header />
      <Profile />
    </div>
  );
}
